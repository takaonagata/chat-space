.side-bar
  .side-header
    .side-header__box
      = current_user.name
      %p.side-header__box__username 
      %ul.side-header__box__menu
        %li.side-header__box__edit-header__box__user
          = link_to "#" do
            = icon('fas','edit')
        %li.side-header__box__new-group
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
  .groups
    = link_to '#', class:"group" do
      .group
        %p.group__group-name test
        %p.group__latest-message まだメッセージがありません